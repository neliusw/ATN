{
  "name": "trustctl",
  "version": "0.1.0",
  "description": "ATN CLI tool",
  "main": "dist/index.js",
  "bin": {
    "trustctl": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node dist/cli.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.0.4",
    "@types/node": "^20.10.5"
  },
  "dependencies": {
    "@atn/core": "*",
    "commander": "^11.1.0"
  }
}
