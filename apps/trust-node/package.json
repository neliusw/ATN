{
  "name": "trust-node",
  "version": "0.1.0",
  "description": "ATN REST API server",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc && node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.0.4",
    "@types/node": "^20.10.5",
    "@types/express": "^4.17.21"
  },
  "dependencies": {
    "@atn/core": "*",
    "express": "^4.18.2"
  }
}
